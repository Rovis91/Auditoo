# Design references (`base/`)

Screenshots, scraped tokens (e.g. CSS variables), and other **design inputs** used to steer a consistent UI for this take-home. These assets are **not** part of the production runtime bundle; they document how the design system was informed.
